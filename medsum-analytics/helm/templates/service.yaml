apiVersion: v1
kind: Service
metadata:
  name: {{ include "transcript-analysis-api.fullname" . }}
  labels:
    {{- include "transcript-analysis-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transcript-analysis-api.selectorLabels" . | nindent 4 }}
